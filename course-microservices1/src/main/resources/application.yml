server:
  port: 8080
  servlet:
    context-path: /api

spring:
  application:
    name: course
  datasource:
    url: jdbc:mysql://localhost:3306/microservicos?allowPublicKeyRetrieval=true&sslMode=DISABLED
    username: admin
    password: tipmuch

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

  jmx:
    enabled: false